<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="android:colorPrimaryDark">@color/aventon_background_color</item>
        <item name="android:colorPrimary">@color/aventon_primary_color</item>
        <item name="android:colorAccent">@color/aventon_primary_color</item>
        <item name="android:navigationBarColor">@color/aventon_primary_color</item>
        <item name="android:windowBackground">@color/aventon_primary_color</item>
        <item name="android:colorForeground">@color/aventon_primary_color</item>
        <item name="android:colorButtonNormal">@color/aventon_secondary_color</item>
        <item name="android:textColorPrimary">@color/aventon_primary_color</item>
        <item name="android:textColorSecondary">@color/aventon_text_color</item>

        <item name="android:color">@color/aventon_third_color</item>


        <item name="android:actionBarTabStyle">@android:style/Widget.Material.ActionBar.TabBar</item>

        <!-- Font style -->
        <item name="android:textStyle">bold</item>

    </style>
    <!--&lt;!&ndash; Base application theme. &ndash;&gt;-->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
        <!--&lt;!&ndash; Customize your theme here. &ndash;&gt;-->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    <!--</style>-->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- AWS -->
    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

</resources>
